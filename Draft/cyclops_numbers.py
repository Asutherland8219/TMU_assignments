# looking for a singular 0 number between two equal sides 

def is_cyclops(n):
    n_list = []
    str_n = str(n)
    
    for x in str_n:
        n_list.append(x)
        
    
    #check length 
    l = len(str_n)
    
    mid = 0
    
    for item in n_list:
        if item != '0':
            mid += 1
        else:
            break
    if 
            
    